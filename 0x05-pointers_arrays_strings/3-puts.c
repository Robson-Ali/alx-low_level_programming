#include "main.h"

/**
 * _puts - prints a string
 * @str: string to print
 *
 * Description: prints a string
 * Return:
 */

void _puts(char *str)

{
	for (; *str != '\0' str++)
	{
		putchar(*str);
		i++;
	}
	putchar('\n');
}
